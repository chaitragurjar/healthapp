import React from 'react';

const ClaimFilingPage = () => {
  return (
    <div>
      <h1>File a Claim</h1>
      <button>Select Scheme (PM-JAY/MJP-JAY)</button>
      <button>Upload Documents</button>
      <button>Submit Claim</button>
    </div>
  );
};

export default ClaimFilingPage;
